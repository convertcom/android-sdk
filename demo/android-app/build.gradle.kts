/*
 * Convert Android SDK Demo App — root build
 * Copyright (c) 2026 Convert Insights, Inc.
 * License: Apache-2.0
 *
 * Empty root build — the only Gradle module is `:app`. Plugins are
 * declared at the :app level to keep the wiring local and
 * easy-to-copy for developers who want to scaffold their own demo.
 */

plugins {
    // No plugins applied at the root.
}
