# Demo Android App

Placeholder. The full demo app lands in Epic 7 (Story 7.1).

When implemented, the demo will be a standalone Android project that discovers the SDK via Gradle's `includeBuild("../../")` mechanism — it is intentionally **not** part of the SDK build (not in `settings.gradle.kts`).
